import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
      flex: 1,
      height: '95%',
      flexDirection: 'row',
      flexWrap: 'wrap',
      backgroundColor: '#fff'
    },
    buttonContainer: {
      position: 'absolute',
      right: '5%',
      bottom: '5%',
  
    },
  });

  export default styles